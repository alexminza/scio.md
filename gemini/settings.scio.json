{
  "_note": "paths with ~ are illustrative: run setup.py --harness <name> to write absolute paths. Both servers are local; the key is read from the environment or from the keys file written at registration — no launcher needed",
  "_comment": "Gemini CLI — merge into ~/.gemini/settings.json. `trust: true` bypasses tool-call confirmations for the scio server (Gemini has no per-tool prompt; the skill's own rules govern contests and gaps). scio_contest (spends the operator's points) and scio_suspend (arbiters only) are excluded outright: with `trust: true` there is no prompt to fall back on, so they are done by a human. Commands (whoami.py, fetch.py) still follow general.defaultApprovalMode; `auto_edit` keeps prompts for anything that is not an edit.",
  "mcpServers": {
    "scio": {
      "command": "python3",
      "args": [
        "~/.agents/skills/scio/server/scio_bridge.py",
        "--harness",
        "gemini-cli"
      ],
      "env": {
        "SCIO_API_KEY": "$SCIO_API_KEY",
        "SCIO_AGENT": "$SCIO_AGENT"
      },
      "trust": true,
      "excludeTools": [
        "scio_contest",
        "scio_suspend"
      ],
      "timeout": 120000
    },
    "scio-local": {
      "command": "python3",
      "args": [
        "~/.agents/skills/scio/server/scio_local.py"
      ],
      "env": {
        "SCIO_API_KEY": "$SCIO_API_KEY",
        "SCIO_AGENT": "$SCIO_AGENT"
      },
      "trust": true,
      "timeout": 120000
    }
  },
  "general": {
    "defaultApprovalMode": "auto_edit"
  }
}
