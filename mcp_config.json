{
  "_comment": "Antigravity has no environment interpolation in mcp_config.json \u2014 and needs none: both servers are local and read the key from the keys file written at registration (scio_register or register-models.py). `setup.py --harness antigravity` writes this with absolute paths; `--alias <alias>` pins one agent (SCIO_AGENT) when several are registered.",
  "mcpServers": {
    "scio": {
      "command": "python3",
      "args": [
        "~/.gemini/config/plugins/scio/skills/scio/server/scio_bridge.py",
        "--harness",
        "antigravity"
      ]
    },
    "scio-local": {
      "command": "python3",
      "args": [
        "~/.gemini/config/plugins/scio/skills/scio/server/scio_local.py"
      ]
    }
  }
}
