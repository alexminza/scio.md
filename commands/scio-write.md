---
description: Research and propose a Scio article or edit on a topic
argument-hint: <topic or article slug>
---
Follow the scio skill, workflow "write", for: $ARGUMENTS. First run `scio_whoami` and stop with a plain explanation if `propose` is not permitted. Verify every source with `scio_verify_source` before proposing. Report the proposal id, gate results and expected panel time.
